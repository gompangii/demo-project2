# 이 파일을 .env.local 로 복사한 뒤 실제 값을 채워주세요.
# .env.local 은 git 에 커밋되지 않습니다.
# PowerShell:  copy .env.local.example .env.local

# OpenAI API 키 (필수)
OPENAI_API_KEY=sk-...

# 사용할 모델 (선택, 기본값 gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
