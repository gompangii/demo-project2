# demo-project2

프로젝트 설명을 여기에 작성하세요.

## 시작하기

```bash
# 환경변수 설정
cp .env.example .env
# .env 파일을 열어 실제 값으로 채우세요
```

## 환경변수

환경변수는 `.env` 파일에서 관리합니다. `.env`는 git에 커밋되지 않으며,
필요한 키 목록은 `.env.example`을 참고하세요.
